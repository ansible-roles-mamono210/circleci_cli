---
driver:
  name: docker
platforms:
  - name: instance
    image: tomonorimatsumura/ansible-test:${MOLECULE_DISTRO:-centos8}-latest
    pre_build_image: True
provisioner:
  name: ansible
verifier:
  name: ansible
scenario:
  test_sequence:
    - syntax
    - create
    - converge
    - idempotence
    - verify
